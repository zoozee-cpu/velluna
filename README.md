# VELLUNA — Laboratory of the Liminal

## Quick start
```bash
npx create-next-app@latest velluna --typescript --tailwind --eslint --app --yes
cd velluna
rm -rf app/globals.css app/page.tsx
# Replace with the files I'm giving you
npm install @react-three/fiber @react-three/drei @react-three/postprocessing framer-motion lenis zustand sonner lucide-react recharts tailwind-merge class-variance-authority clsx
npm run dev
